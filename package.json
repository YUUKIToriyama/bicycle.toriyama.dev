{
  "scripts": {
    "build:code": "rimraf ./dist && tsc",
    "build:article": "rimraf ./public && node dist/main.js"
  },
  "devDependencies": {
    "@types/node": "^16.11.6",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.4"
  },
  "dependencies": {
    "zenn-markdown-html": "^0.1.98"
  }
}
