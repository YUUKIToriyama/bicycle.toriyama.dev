{
  "scripts": {
    "start": "http-server -p 1234 ./public",
    "build:article": "rimraf ./public && node src/main.js"
  },
  "devDependencies": {
    "@blogmd/core": "^0.1.0",
    "@blogmd/styles": "^0.0.9",
    "http-server": "^14.0.0",
    "rimraf": "^3.0.2"
  }
}
